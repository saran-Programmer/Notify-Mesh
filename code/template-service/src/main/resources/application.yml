spring:
  application:
    name: template-service

server:
  port: 9093

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui

aws:
  region: ap-south-1
  dynamodb:
    table-name: templates
